[project]
name = "incan_pub"
version = "0.1.0"
description = "incan.pub v0: admission, projection and the publishing verbs of the interim registry"
requires-incan = ">=0.5.1,<0.6.0"

[project.scripts]
main = "src/main.incn"
